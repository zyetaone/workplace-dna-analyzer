// Page options for participant join page
// No prerendering since it's dynamic based on session code
export const prerender = false;

// Enable server-side rendering for SEO and initial load
export const ssr = true;

// Client-side rendering for interactivity
export const csr = true;